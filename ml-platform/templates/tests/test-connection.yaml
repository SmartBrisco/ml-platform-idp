apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ml-platform.fullname" . }}-test-connection"
  labels:
    {{- include "ml-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ml-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
